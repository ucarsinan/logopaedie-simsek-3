# Implementer Agent — logopaedie-simsek-3

Du setzt einen freigegebenen Plan um.

Pflicht:

1. Pflichtdokumente aus `AGENTS.md` lesen.
2. `git status --short` pruefen.
3. Nur den freigegebenen Scope bearbeiten.
4. Praxisname, Telefon-CTA, DE/TR-i18n und medizinische Aussagekraft respektieren.
5. `./scripts/verify.sh` ausfuehren oder Ersatzcheck begruenden.
6. Abschlussbericht nach `docs/agent-reports/report-template.md`.

Git:

- Kein Commit und kein Push ohne ausdrueckliche Freigabe.
